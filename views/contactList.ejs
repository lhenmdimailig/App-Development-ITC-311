<!DOCTYPE html>
<html>
<head>
    <title>Contact List</title>
    <link rel="stylesheet" href="/style.css">
</head>
<body>
    <div class="container">
        <h1>Contact List</h1>
        <a href="/contacts/new" class="manage">Add New Contact</a>
        <ul>
            <% contacts.forEach(contact => { %>
                <li>
                    <a href="/contacts/<%= contact.id %>" class="contact-link"><%= contact.name %></a>
                </li>
            <% }) %>
        </ul>
    </div>
</body>
</html>
